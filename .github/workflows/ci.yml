name: CI

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
  workflow_dispatch: {}

jobs:
  build-android:
    runs-on: windows-latest
    strategy:
      matrix:
        configuration: [Release]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      # ⚡ Cache .NET SDK + MAUI workloads
      - name: Cache .NET workloads
        uses: actions/cache@v4
        with:
          path: |
            ~\.dotnet
          key: dotnet-${{ runner.os }}-maui-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            dotnet-${{ runner.os }}-maui-

      # 🔑 Android-only workload (Windows-safe)
      - name: Install MAUI Android workload
        run: dotnet workload install maui-android

      # ✅ Restore ONLY Android TFM
      - name: Restore (Android)
        run: dotnet restore src/Plugin.Maui.TvDPad/Plugin.Maui.TvDPad.csproj -f net10.0-android

      # ✅ Build ONLY Android TFM
      - name: Build (Android)
        run: dotnet build src/Plugin.Maui.TvDPad/Plugin.Maui.TvDPad.csproj `
          -f net10.0-android `
          -c ${{ matrix.configuration }} `
          --no-restore

      - name: Test
        run: dotnet test src/tests/Plugin.Maui.TvDPad.Tests/Plugin.Maui.TvDPad.Tests.csproj `
          -c ${{ matrix.configuration }} `
          --no-build `
          --verbosity normal
